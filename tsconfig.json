{
  "compilerOptions": {
    // ts 컴파일 후 출력 결과물이 저장되는 위치
    "outDir": "./dist",
    // 모듈 방식(es, export/import)
    "module": "ESNext",
    // ts 를 js 로 컴파일 한 후 결과 버전
    // exbuild-loader 쓸 때는 영향을 안받음
    "target": "ES2020",
    // ts에서 사용할 기본 js 라이브러리
    "lib": ["ESNext", "DOM"],
    // ts에서 js 코드를 쓸 수 있는지 여부
    "allowJs": true,
    // jsx 라고 문법을 처리하는 방법(js로 변환했을 때)
    // javascripts Virtual DOM 객체를 HTML처럼 표기하는 방법
    // exbuild-loader 쓸 때는 영향을 안받음

    // jsx-transfile: React import를 안해도되게 하는 방법
    // 어디까지나 작성하는 컴포넌트 코드에 대한 이야기
    // 어디선가 글로벌 수준에 React import를 해야한다.
    "jsx": "preserve"
  }
}